本目录用于“构图（9类）”分类模型的训练、导出与本地验证，产物为后端推理使用的 ONNX 权重。整体流程：基于标注数据训练分类头，使用不均衡权重与常规增强；推理侧与 Go 端保持一致的 letterbox/归一化，确保线上线下结果可对齐。train.py：数据集读取、预处理（等比缩放+padding）、训练与保存最优 pth；exp.py：将最佳 pth 导出为 comp_model.onnx，并固定输入/输出节点名；test.py：单图推断、Sigmoid 与阈值筛选，用于快速验收类别与置信度；readme.md：目录说明。
